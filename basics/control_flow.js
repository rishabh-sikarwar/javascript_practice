// different types of control flow are those which decide that which part of the code should be executed or not 

// 1.  if

 // 2 == "2"              //true
  //2 === "2"             //false  check the datatype also it is in strict checking
    //!==                 //strict checking in anti pattern 








    // const score = 200 

    // if(score > 100){
    //   const power = "fly" 
      
    //   console.log(User power: ${power});

    // }    
    // console.log(User power: ${power});





    // *************************************short hand notation *****************************************
    const balance = 1000
    if (balance > 500) console.log("test");          //in this we can not write multiple lines 
    if (balance > 500) console.log("test"),
    console.log("test2");                             //by this we can write multiple lines code   we can use comma

    


